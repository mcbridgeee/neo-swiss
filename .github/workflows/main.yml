name: Eleventy Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: is117/neo-swiss
        run: npm install

      - name: Build Eleventy
        working-directory: is117/neo-swiss
        run: npx @11ty/eleventy --output=_site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: is117/neo-swiss/_site

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
