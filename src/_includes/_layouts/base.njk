<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="neo-swiss lineage site — a meta study of neo-swiss design by Bridget McTiernan" />
    <title>{{ title | default("Swiss Lineage Gallery") }}</title>
    <link rel="icon" href="{{ '/favicon.svg' | url }}" type="image/svg+xml" />
    <link rel="stylesheet" href="{{ '/css/main.css' | url }}" />
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to main content</a>
    <div class="page">
      {% include "header.njk" %}
      <main id="main">
        {% block content %}{% endblock %}
      </main>
      {% include "footer.njk" %}
    </div>
  </body>
</html>
